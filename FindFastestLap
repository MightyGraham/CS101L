import csv
import math
#i means initial
#find where speed reaches 0, "throw it out"
#find where 1st point "crosses"
#distance: square root of (x2 - x1) **2 + (y2 - y1)**2
#for loop: (xi + 1 - xi)
#when x > xi
#then for loop that starts at first row

with open('_GPS.csv') as distance:
    reader = csv.reader(distance)
    for row in reader:
        initial_Latitude = '43.07297'
        initial_Longitude = '-77.1718'
        X = row[2]
        Y = row[3]
        time = row[0]
        speed = row[5]
        #total = initialX + 1 - initialX
        if X > initial_Latitude:
            print(X)
            if time > '614':
                pass
            
        
